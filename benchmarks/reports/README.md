# reports

Final benchmark reports and derived summaries.

Each report should record:

- Host, CPU, GPU, OS, driver, CUDA, compiler, and environment metadata.
- Exact command lines and raw output paths.
- Thread count, NUMA policy, GPU power limit, clocks, and thermal state.
- Summaries for floating-point throughput, bandwidth, and microbenchmark
  results.
